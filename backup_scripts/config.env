DB_CONTAINER="db"
NEXTCLOUD_CONTAINER="app"
NEXTCLOUD_VOLUME="nextcloud_nextcloud"
DB_VOLUME="nextcloud_db"
DB_USER="nextcloud"
DB_NAME="nextcloud"
RESTIC_PASSWORD="password123"

# Resolves to the "backups" folder located one level above the script
BACKUPS_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../backups" && pwd)"